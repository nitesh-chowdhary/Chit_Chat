namespace API {
  export const baseURL = '';
  export const endpoint = {
    auth: {
      login: '',
      register: '',
      otp: '',
    },
  };

  export const cacheKey = {
    user: 'user',
  };
}

export default API;
